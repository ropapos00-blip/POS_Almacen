services:
  - type: web
    runtime: static
    name: lickan42
    buildCommand: pnpm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
